# Development dependencies. Install with:  pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8,<9        # test runner (see tests/ — to be added)
ruff>=0.6           # linter + formatter
