# Grafana alerting — notification policy: route everything to the email contact point.
apiVersion: 1

policies:
  - orgId: 1
    receiver: radonaix-ops
    group_by: [alertname]
    group_wait: 30s
    group_interval: 5m
    repeat_interval: 4h
